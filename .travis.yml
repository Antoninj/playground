language: python
python:
  - "3.7.5"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script: pytest
deploy:
  provider: heroku
  api_key:
    secure: ""
  app: co-detector